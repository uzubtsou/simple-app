apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: backend
spec:
  chart:
    spec:
      version: 6.7.0
  values:
    replicaCount: 2
    ui:
      color: "#3EB489"
      message: "Backend qa"
