apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: backend
spec:
  chart:
    spec:
      version: 6.8.0
  values:
    replicaCount: 1
    ui:
      color: "#34577c"
      message: "Backend dev"
    logLevel: debug
