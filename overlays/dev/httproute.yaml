apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: podinfo
spec:
  parentRefs:
    - name: sandpit
      namespace: istio-ingress
  hostnames:
    - dev-info.sand.pit.im
  rules:
    - backendRefs:
        - name: podinfo
          port: 8080
