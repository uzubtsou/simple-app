apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: podinfo
  namespace: podinfo-dev
spec:
  parentRefs:
    - name: sandpit
      namespace: istio-ingress
  hostnames:
    - podinfo.sand.pit.im
  rules:
    - backendRefs:
        - name: dev-podinfo
          port: 8080
